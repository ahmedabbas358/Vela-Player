import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Dark background palette
  static const Color background = Color(0xFF0D0F14);
  static const Color surface = Color(0xFF161A22);
  static const Color surfaceVariant = Color(0xFF212631);
  static const Color surfaceElevated = Color(0xFF2B3241);

  // Vibrant primary & accents
  static const Color primary = Color(0xFF6366F1); // Indigo
  static const Color primaryLight = Color(0xFF818CF8);
  static const Color accent = Color(0xFF06B6D4); // Cyan
  static const Color accentNeon = Color(0xFF10B981); // Emerald
  static const Color amberHighlight = Color(0xFFF59E0B); // Anime Gold

  // Text colors
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Glassmorphic overlays
  static const Color glassBackground = Color(0xB3161A22);
  static const Color glassBorder = Color(0x33FFFFFF);
}

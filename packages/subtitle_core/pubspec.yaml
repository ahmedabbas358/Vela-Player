name: subtitle_core
description: High-performance pure Dart subtitle parsing, Unified Subtitle Model, health repair heuristics, and timeline synchronization.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0

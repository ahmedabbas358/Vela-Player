name: design_system
description: Vela Design System - Modern tokens, typography, dark/light themes, and custom media player components.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ">=6.0.0 <9.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^4.0.0
